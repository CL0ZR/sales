 🚀 نظام التهيئة التلقائية - دليل الإعداد

 📋 نظرة عامة

نظام التهيئة التلقائية يضمن أن يبدأ التطبيق دائمًا في حالة جاهزة للعمل، دون الحاجة لأي تدخل يدوي من المستخدم.

 ✨ الميزات

 1️⃣ إنشاء المجلد تلقائيًا
- عند أول تشغيل: إنشاء المجلد `D:\MyWarehouseData\` تلقائيًا
- إنشاء مجلدات فرعية:
  - `D:\MyWarehouseData\` - قاعدة البيانات
  - `D:\MyWarehouseData\backups\` - النسخ الاحتياطية
  - `D:\MyWarehouseData\exports\` - ملفات التصدير

 2️⃣ قاعدة بيانات تلقائية
- إنشاء قاعدة البيانات `warehouse.db` تلقائيًا
- إنشاء الجداول المطلوبة تلقائيًا
- تفعيل WAL mode للأداء العالي

 3️⃣ حساب المدير الافتراضي
- إنشاء حساب المدير تلقائيًا إذا لم يكن موجودًا
- **اسم المستخدم:** `super`
- **كلمة المرور:** `Moh@9801`
- **الصلاحية:** `admin`

 4️⃣ تشفير كلمات المرور
- استخدام `bcrypt` لتشفير كلمات المرور
- حماية أمنية قوية

 🔄 عملية التهيئة

 في أول تشغيل:
```
1. فتح التطبيق
2. التهيئة التلقائية تبدأ
3. إنشاء المجلد D:\MyWarehouseData\
4. إنشاء قاعدة البيانات
5. إنشاء حساب المدير (super/Moh@9801)
6. ظهور رسالة الترحيب مع بيانات الدخول
7. جاهز للاستخدام!
```

 في التشغيلات التالية:
```
1. فتح التطبيق
2. التحقق من وجود المجلد/القاعدة
3. لا تهيئة مطلوبة
4. جاهز للعمل!
```

 🛠️ الملفات المهمة

 `src/lib/database.ts`
- `ensureDatabaseSetup()` - التأكد من إنشاء المجلدات والجداول

 `src/lib/queries/users.ts`
- `createDefaultAdminIfNeeded()` - إنشاء حساب المدير إذا لم يكن موجودًا

 `src/app/api/setup/route.ts`
- واجهة API لعملية التهيئة التلقائية

 `src/hooks/useAutoSetup.ts`
- Hook يدير عملية التهيئة عند بدء التطبيق

 `src/components/FirstTimeSetupNotice.tsx`
- نافذة منبثقة تظهر بيانات الدخول لمرة واحدة

 📊 هيكل قاعدة البيانات

 جدول المستخدمين (users):
```sql
- id: TEXT PRIMARY KEY
- username: TEXT UNIQUE NOT NULL
- password: TEXT NOT NULL (مشفرة)
- role: TEXT DEFAULT 'user' ('admin' أو 'user')
- fullName: TEXT
- email: TEXT
- phone: TEXT
- isActive: INTEGER DEFAULT 1
- createdAt: TEXT DEFAULT CURRENT_TIMESTAMP
- updatedAt: TEXT DEFAULT CURRENT_TIMESTAMP
- lastLogin: TEXT
```

 🔐 الأمان

- كلمات المرور مشفرة باستخدام `bcrypt`
- الحساب الافتراضي موصى بتغييره بعد أول استخدام
- نظام صلاحيات (admin/user)

 🚨 تنبيهات أمنية

⚠️ **ينصح بشدة بتغيير كلمة المرور الافتراضية بعد أول تسجيل دخول**

⚠️ **غير مسار التخزين الافتراضي إذا لزم الأمر**

 🛠️ استكشاف الأخطاء

 إذا لم يظهر حساب المدير:
1. تحقق من وجود قاعدة بيانات في `D:\MyWarehouseData\`
2. تحقق من تشغيل الخادم (`npm run dev`)
3. افتح وحدة تحكم المتصفح (F12) للتحقق من الأخطاء

 إذا فشلت التهيئة:
1. تأكد من صلاحيات الكتابة على محرك D:
2. تحقق من أن الخادم قيد التشغيل
3. راجع سجلات الخادم

 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع سجلات الخادم
2. تحقق من وحدة تحكم المتصفح
3. تأكد من أن `D:\MyWarehouseData\` قابل للكتابة